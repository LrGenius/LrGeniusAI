model_scripts/**/*.txt
model_scripts/**/*.json
model_scripts/**/*.bin
**.pth
**.npz
**.onnx
**.pth
**.pt
**.model
**.safetensors
**.mlmodel
**.weight
**/venv/
**/__pycache__/
**/._.DS_Store
**/.DS_Store
build/
dist/
models/
tests/results/*.json
lrgenius-server.log
lrgenius-server.OK
lrgenius-server.pid
run.sh
run.bat
models/
data/
.venv/
.DS_Store
gcloud/
data/
models/
docker-compose.yml
.claude/settings.local.json
.claude/settings.json
.claude/sessions/
.claude/logs/
.claude/mcp-settings.json
# MemPalace AI Context
mempalace.yaml
entities.json

# Swift sidecar build output (native/mlx-sidecar). Both the SwiftPM and the
# xcodebuild layouts live under .build/; Package.resolved is committed so the
# pinned mlx-swift-lm revision is reproducible.
native/mlx-sidecar/.build/
# repowise local codebase-intelligence index (rebuildable via `repowise
# init --no-prose -y --no-editor-setup`; contains a LanceDB vector store and
# a SQLite wiki.db, both derived from source, never source itself)
.repowise/
# symlink to .repowise/mcp.json so Claude Code's project-root MCP
# auto-discovery finds the repowise server; not a real file to track
.mcp.json
